You are an expert PyTorch optimizer engineer performing a MUTATION on an existing optimizer.

You will receive:
- The parent optimizer's IDEA_DNA (list of core ideas)
- The parent's evolution log (history of changes and scores)
- The parent's current code (editable sections only)
- The parent's current score

Your task: modify one or more ideas in the DNA to create an improved variant. You may:
- Change an existing idea (e.g., switch momentum scheme, adjust scaling strategy)
- Add a new idea (e.g., introduce gradient noise, add a warmup phase)
- Remove an idea that seems unhelpful
- Combine or refine existing ideas

Hard requirements for the optimizer code:
1) The optimizer is a Python class with exactly these methods:
   - __init__(self, model: nn.Module, max_steps: int)
   - step(self, weights, grads, activations, step_fn) -> None
   - zero_grad(self, set_to_none: bool = True) -> None

2) __init__ receives the full model and the total step budget.

3) step receives:
   - weights: dict[str, torch.Tensor] — current parameter values (detached)
   - grads: dict[str, torch.Tensor] — current gradients (detached)
   - activations: dict[str, torch.Tensor] — layer activations (detached)
   - step_fn: callable — calling step_fn() does another forward-backward pass,
     returns (loss: float, grads: dict, activations: dict).
     Each call costs one step from the global budget. Use sparingly.

4) Use only standard library + torch (+ optional numpy).

Response format — return EXACTLY these sections:

## IDEA_DNA
Updated semicolon-separated list of core ideas (modified from parent).

## CHANGE_DESCRIPTION
One paragraph explaining what you changed and why.

## IMPORTS
Python import statements (do NOT include `import torch` or `import torch.nn as nn`).

## INIT_BODY
The body of __init__ (indented with 8 spaces).

## STEP_BODY
The body of step (indented with 8 spaces).

## ZERO_GRAD_BODY
The body of zero_grad (indented with 8 spaces).
