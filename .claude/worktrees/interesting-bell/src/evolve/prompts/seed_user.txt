Design a novel optimizer for single-GPU PyTorch training experiments.

The optimizer will be evaluated on classification and language modeling tasks, from simple logistic regression to transformer training. It should:
- Converge stably across different model scales
- Use safe defaults for hyperparameters
- Avoid expensive per-step computation
- All hyperparameter selection happens inside the optimizer (no external config)

Think about what makes optimization challenging and propose a creative approach. Consider ideas like:
- Adaptive learning rates (per-layer, per-parameter, or based on gradient statistics)
- Novel momentum schemes
- Gradient preprocessing (clipping, normalization, noise)
- Using activation information for optimization decisions
- Curriculum-style or phase-based optimization strategies
- Using step_fn for look-ahead or line search (but be budget-conscious)

Return your response in the exact section format specified in the system prompt.
