You are designing one brand-new combinatorial-optimization organism from scratch.

Priority order:
1. invent a coherent sectioned genetic code with a plausible high-score mechanism for the supplied task
2. ensure each idea is placed in the correct genome section
3. summarize the single defining novelty

Think in genes first, implementation second.

The genome is sectioned. Every gene must live in the correct section of `## CORE_GENES`.
The section schema is authoritative. Use each section only for the kind of content its description allows.

Important:
- the organism must implement the task contract supplied in the per-task project context: a `solve(**kwargs) -> dict` whose returned dict carries the required output keys for that task
- the goal is still to design an organism at the level of ideas, assumptions, constructive principles, and local procedures
- the organism must define a plausible causal route from construction through neighborhood, feasibility, repair, search, and control to a high valid normalized score
- prefer a lean organism with one strong mechanism over a rich organism with many loosely coupled refinements
- avoid introducing more than one main search idea, one main repair idea, and one main neighborhood idea unless tighter coupling is essential
- avoid designs that make implementation solve a research puzzle; the construction and search must be concretely realizable in deterministic Python within the per-instance timeout
- respect the wall-clock timeout: state how the construction and search stay bounded so `solve` always returns a feasible solution in time
- do not claim a solution is feasible unless the construction, feasibility model, and repair policy make that true for the constraints named in the task project context
- if `OPTIONAL_CODE_SKETCH` describes a procedure, it must implement the same mechanism named in the relevant section; otherwise use `- None.`
- specify one directly executable deterministic path, not a list of alternatives, fallback branches, or exploratory "try this then maybe that" choices
- state one intended construction family and one intended search regime; if the organism keeps a pure greedy construction with no improvement search, justify why that should win instead of a construction-plus-local-search alternative
- do not rely on a generic greedy construction plus generic local search as an implicit safe default; the genes should explain what makes this organism's regime score-bearing
- each required section should contribute to that route
- section completeness alone is not enough; each required section should contribute to the effective solving behavior
- avoid organisms whose sections are formally coherent but behaviorally weak, generic, or decorative
- do not fill sections with generic filler text; every non-optional section should change the effective solving behavior
- do not jump directly to a finished solver recipe
- do not replace the genetic code with a hand-tuned solved arrangement
- do not treat the schema as decoration; it defines the structure of the genome
- keep genes atomic within each section: one independent, testable idea per bullet
- if a section allows local procedural detail or a local code-like sketch, keep it narrow and local to that section only
- no section may contain a full end-to-end implementation
- `CHANGE_DESCRIPTION` must remain plain-language only and must not contain code

## Response format

Return the structured artifact directly.
The first line of your answer must be `## CORE_GENES`.
Do not add analysis, commentary, or prefatory text before the required sections.
Do not use markdown code fences anywhere in the response.
The response is invalid if it stops after `## CORE_GENES`; always continue with `## INTERACTION_NOTES`, `## COMPUTE_NOTES`, and `## CHANGE_DESCRIPTION`.

Return exactly these top-level sections, in this exact order:

## CORE_GENES
Inside `## CORE_GENES`, return exactly these subsection headings, in this exact order:

### CONSTRUCTION
### SEARCH_STRATEGY
### NEIGHBORHOOD
### FEASIBILITY_MODEL
### REPAIR_POLICY
### CONTROL_POLICY
### PARAMETERS
### OPTIONAL_CODE_SKETCH

Rules for subsection bodies:
- `CONSTRUCTION` through `PARAMETERS` must each contain at least one `- ` bullet
- `OPTIONAL_CODE_SKETCH` must contain either one or more `- ` bullets, or exactly one bullet `- None.`
- `- None.` is allowed only in `OPTIONAL_CODE_SKETCH`
- use the fewest bullets needed to define the mechanism cleanly
- avoid filling a section with multiple near-duplicate bullets
- for seed organisms, prefer `- None.` in `OPTIONAL_CODE_SKETCH`; if it is used, keep it to at most two local pseudocode bullets and never use code fences
- if a gene needs multi-line continuation, continue the same bullet using lines indented by two spaces
- do not use nested headings inside subsection bodies

## INTERACTION_NOTES
State what assumptions the organism makes, how the sections work together, and where the design is likely to help or fail.

## COMPUTE_NOTES
State the computational style implied by the design, including how it stays within the per-instance timeout.

## CHANGE_DESCRIPTION
Give one unified novelty summary for this organism.
Include:
(1) what core hypothesis and solving approach this organism tests
(2) why this approach should produce a good normalized score
Before writing it, check that every named mechanism in `CHANGE_DESCRIPTION` appears in the relevant `CORE_GENES` section and is not contradicted by another section, and that the named score path is not merely a generic greedy-plus-local-search fallback with parameter tuning.
This must be plain-language only. No code, no code-like snippets, no implementation dump.
