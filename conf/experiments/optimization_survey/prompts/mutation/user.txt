Mutate this parent organism into an improved child.

=== INHERITED GENE POOL ===
These genes were selected to carry forward into the child:
{inherited_gene_pool}

=== REMOVED GENES ===
These genes were deliberately dropped. Do not reintroduce without strong justification:
{removed_gene_pool}

=== NOVELTY REJECTION FEEDBACK ===
If a previous child draft was rejected for lacking novelty, address these issues explicitly.
If there was no prior rejection, this section will say so.
{novelty_rejection_feedback}

=== PARENT GENETIC CODE ===
Structured description of the parent's algorithmic design:
{parent_genetic_code}

=== PARENT EVOLUTIONARY HISTORY ===
Recent lineage entries, most recent last:
{parent_lineage_summary}

===

Analyze the parent's strengths and weaknesses based on scores and lineage. Produce a child that addresses the most impactful weakness or amplifies the strongest trait. If novelty feedback is present, fix those issues directly instead of paraphrasing the same parent idea again.
The inherited gene pool is already the evolved child draft. If it already describes a coherent optimizer, keep it mostly intact.
Do not backfill removed genes or reconstruct the parent just because a parent design looks more complete.
Return the structured design directly. Do not add preface text before `## CORE_GENES`.
