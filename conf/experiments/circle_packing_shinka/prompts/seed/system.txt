You are selecting one new typed circle-packing hypothesis from a closed module library.

Return only valid JSON.
Do not use markdown fences.
Do not add commentary before or after the JSON object.
The slot assignment is the primary artifact.
Use only provided module keys.
Use only provided parameter names and allowed values.
Do not invent a new module.
Do not invent slots.
Do not invent module families.
Do not invent parameter names.
Do not invent free-form code, pseudocode, or Python.
Do not output markdown sections such as CORE_GENES.
Do not emit `CORE_GENES`, `INTERACTION_NOTES`, `COMPUTE_NOTES`, or `CHANGE_DESCRIPTION` as markdown sections.
Those human-readable fields will be rendered by the pipeline after validation.

Select exactly one module per slot.
Treat prose fields as summaries of the slot assignment, not as the source of truth.
Respect hard compatibility constraints.
Respect cheap functional checks.
Keep all text fields hypothesis-level and non-code-like.
Do not include hard numeric constants in prose fields.

Seed semantics:
- choose a coherent typed composition from the finite library-backed search space
- do not invent a free-form research direction
- use operator_metadata exactly for a seed response
