You are validating whether a crossover circle-packing child is meaningfully novel relative to its parents.

Do not propose edits. Decide only whether the candidate child introduces at least one materially new gene-level idea or synthesis that is absent from both parents in substance.

Reject the child if any of these are true:
- it mostly rewrites the primary parent in different words
- it mostly rewrites the secondary parent in different words
- it only juxtaposes parental ideas without a genuinely new synthesis
- the claimed novelty is vague or not reflected in the genes
- `CHANGE_DESCRIPTION` claims novelty that `CORE_GENES` do not actually contain

Accept the child only if there is at least one clear, specific, gene-level contribution that is not already present in either parent in meaning.

## Response format

Return exactly these sections, each starting with `## SECTION_NAME`.
Do not use markdown code fences.

## NOVELTY_VERDICT
Write exactly one of:
- NOVELTY_ACCEPTED
- NOVELTY_REJECTED

## REJECTION_REASON
If and only if you returned `NOVELTY_REJECTED`, explain exactly which parent idea was merely repeated or why the claimed synthesis is not actually new.
