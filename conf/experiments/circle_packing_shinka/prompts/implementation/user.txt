Compile this circle-packing organism into scaffold region bodies.

=== FUNCTION CONTRACT ===
You must implement exactly this function:

    def run_packing() -> tuple[np.ndarray, np.ndarray, float]:

Arguments: none.
Returns a 3-tuple:
  1. centers: np.ndarray of shape (26, 2) — each row is (x, y) center of a circle
  2. radii: np.ndarray of shape (26,) — radius of each circle
  3. reported_sum: float — must equal np.sum(radii)

Constraints enforced by the evaluator:
  - All values must be finite (no NaN, no inf)
  - All radii must be > 0
  - Every circle fully inside [0, 1] x [0, 1]: center[i] - radii[i] >= 0 and center[i] + radii[i] <= 1, for both x and y
  - No overlaps: for every pair i != j, distance(centers[i], centers[j]) >= radii[i] + radii[j]
  - reported_sum must match np.sum(radii) within 1e-6

Score = sum of radii. Higher is better.

Validity preservation note:
  - The final returned packing must satisfy the evaluator constraints
  - If local correction is needed, preserve large feasible radii and existing feasible geometry whenever possible
  - Do not perform a generic end-stage global shrink pass unless the genes themselves call for that behavior

=== COMPILATION MODE ===
{compilation_mode}

=== CHANGED_SECTIONS ===
{changed_sections}

=== ORGANISM GENETIC CODE ===
{organism_genetic_code}

=== ORGANISM CHANGE_DESCRIPTION ===
{change_description}

=== MATERNAL BASE GENETIC CODE ===
{base_parent_genetic_code}

=== MATERNAL BASE IMPLEMENTATION ===
{base_parent_implementation}

=== CANONICAL IMPLEMENTATION SCAFFOLD ===
{implementation_template}

=== TASK ===
Produce scaffold region bodies for this organism.

If COMPILATION_MODE is FULL:
- compile all eight canonical regions from the scaffold

If COMPILATION_MODE is PATCH:
- compile only the listed CHANGED_SECTIONS
- do not compile any unchanged section
- the repository will preserve unchanged maternal regions byte-for-byte
- preserve the maternal organism's effective packing behavior outside the changed sections

Be faithful to the supplied sectioned genes and the change description.
Do not output a full `implementation.py`.
Return only the patch artifact in the required format.
