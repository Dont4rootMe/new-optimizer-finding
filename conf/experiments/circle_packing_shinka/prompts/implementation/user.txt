Compile this organism into code.

=== IMPLEMENTATION INTERFACE ===
You must implement exactly this function:

    def run_packing() -> tuple[np.ndarray, np.ndarray, float]:

Arguments: none.
Return the tuple interface used by the scaffold: `(centers, radii, reported_sum)`.
The supplied genetic code defines the intended geometry, count, feasibility discipline, and objective.
Do not add task facts, evaluator assumptions, target counts, or exact formulas that are absent from the genes.

Validity preservation note:
  - The final returned organism state must satisfy the validity discipline implied by the genes
  - If local correction is needed, preserve the intended geometry and strong inherited structure whenever possible
  - Do not perform a generic end-stage global shrink pass unless the genes themselves call for that behavior

=== COMPILATION MODE ===
{compilation_mode}

=== CHANGED_SECTIONS ===
{changed_sections}

=== ORGANISM GENETIC CODE ===
{organism_genetic_code}

=== ORGANISM CHANGE_DESCRIPTION ===
{change_description}

=== MATERNAL BASE GENETIC CODE ===
{base_parent_genetic_code}

=== MATERNAL BASE IMPLEMENTATION ===
{base_parent_implementation}

=== CANONICAL IMPLEMENTATION SCAFFOLD ===
{implementation_template}

=== TASK ===
Realize the supplied genes faithfully and preserve the objective-seeking behavior they describe.

If COMPILATION_MODE is FULL:
- return the final full Python file only
- use the canonical scaffold as the file skeleton
- do not return a patch artifact
- the first non-empty line must be `import numpy as np` or another Python import, not `FULL`, `PATCH`, `## COMPILATION_MODE`, or a markdown fence
- the file must contain a complete executable `def run_packing():` that returns exactly `(centers, radii, reported_sum)`
- return only the final organism output state, not repeated generated batches, candidate histories, or repair trajectories
- do not write long exploratory comments in place of implementation; choose the simplest faithful deterministic realization and finish the file

If COMPILATION_MODE is PATCH:
- return only the patch artifact in the required `## COMPILATION_MODE` / `## REGION` / `## END_REGION` format
- compile only the listed CHANGED_SECTIONS
- do not compile any unchanged section
- the repository will preserve unchanged maternal regions byte-for-byte
- preserve the maternal organism's effective packing behavior outside the changed sections, including inherited high-score behavior

Be faithful to the supplied sectioned genes and the change description.
Do not output markdown fences or commentary.
