Repair this organism implementation.

=== REPAIR INTERFACE ===
The function you must fix:

    def run_packing() -> tuple[np.ndarray, np.ndarray, float]:

Arguments: none.
It must return the tuple interface used by the scaffold: `(centers, radii, reported_sum)`.
The genetic code and current implementation define the intended task behavior, target structure, and validity discipline.
Do not add hidden evaluator assumptions, target counts, or exact formulas absent from those inputs.

=== ORGANISM GENETIC CODE ===
{organism_genetic_code}

=== ORGANISM NOVELTY SUMMARY ===
{change_description}

=== CURRENT IMPLEMENTATION.PY ===
{current_implementation}

=== CANONICAL IMPLEMENTATION SCAFFOLD ===
Use this as the structural reference if the current file is too broken to preserve.
Return a full Python file, not a region patch.

{implementation_template}

=== FAILING PHASE ===
{phase}

=== FAILING EXPERIMENT ===
{experiment_name}

=== ERROR HISTORY ===
{error_history}

Fix the error while staying faithful to the genetic code.
The full-file output format is only the repair interface contract; preserve unaffected scaffold regions and the inherited mechanism whenever possible.
Repair the smallest affected region set consistent with the error history and preserve the current algorithm outside those regions whenever possible.
If the current implementation already realizes a strong mechanism imperfectly, repair it locally instead of replacing it with a weaker but safer generic algorithm.
If the current file starts with `FULL`, `PATCH`, `## COMPILATION_MODE`, or a markdown fence, remove that preamble and return only Python.
If the failure is that `run_packing()` does not return a 3-tuple, fix the return path first: compute arrays, compute `reported_sum = float(np.sum(radii))`, and return `centers, radii, reported_sum`.
If the failure names specific entities, fix and re-check that exact local issue; if it reports a shape error, return exactly the final center/radius arrays implied by the genes rather than repeated generated batches; if timeout appears, bound the implicated loop.
Do not replace the whole algorithm with a short generic packing unless no local repair is viable.
Return ONLY the corrected full `implementation.py` text. No markdown fences, no commentary.
