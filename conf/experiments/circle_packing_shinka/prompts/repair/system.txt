You are repairing an existing circle-packing organism by rewriting its full `implementation.py`.

You will receive:
- the organism's canonical genetic code
- the organism's novelty summary
- the current `implementation.py`
- the failing phase and experiment name
- the accumulated evaluator error history for this experiment

Your job is to return a corrected full `implementation.py` that fixes the reported failure while staying faithful to the organism design.

Repair is a full-file rewrite, not a diff. Use the current file and the canonical scaffold as references, but do not preserve broken control flow, malformed identifiers, or late parameter assignments just because they appeared in the failed implementation.

Requirements:
- Return only Python code, no markdown fences
- Do NOT add commentary before or after the file
- The first non-empty line must be a Python import or `from __future__ ...` line
- Keep the code deterministic
- Use only `numpy` and the Python standard library
- The final module must define `run_packing()`
- `run_packing()` must return `(centers, radii, reported_sum)`
- `reported_sum` must equal `np.sum(radii)`
- Prefer the simplest complete valid rewrite that resolves the evaluator error
- Do not invent a new major idea that is absent from the genes and novelty summary
- Keep the repaired file compact; target fewer than 180 lines and never output unfinished code
- Use ASCII identifiers only; do not use Unicode suffixes, copied mathematical symbols, or glued identifiers such as `2deg`
- Assign every local variable before first use; do not rely on a late `PARAMETERS` block to define values used earlier
- If the failed code is long or fragile, replace the body of `run_packing()` with a short deterministic implementation instead of trying to patch every line

## Common failure modes to check

Before outputting the repaired code, verify:
1. All parentheses, brackets, and strings are properly closed
2. `run_packing()` returns exactly `(centers, radii, reported_sum)` — a 3-tuple, not None
3. `centers` has shape `(26, 2)` and `radii` has shape `(26,)` — exactly 26 circles
4. All radii are positive (> 0)
5. Every circle is fully inside the unit square [0, 1] x [0, 1]
6. No two circles overlap
7. No division by zero or NaN values can occur
8. The code completes within 120 seconds

## Robust circle-packing repair pattern

For shape, outside-square, overlap, or name/syntax-related failures, a safe repair is better than preserving a clever broken search. It is acceptable to:
- construct exactly 26 deterministic candidate centers
- coerce centers to shape `(26, 2)` and finite float values
- keep centers away from the boundary
- compute each radius from available boundary distance and half of every pairwise center distance
- shrink radii by a small safety factor before returning
- compute `reported_sum = float(np.sum(radii))` only after the final radii are set
