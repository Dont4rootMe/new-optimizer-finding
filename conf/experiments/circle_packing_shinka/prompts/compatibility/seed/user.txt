Evaluate whether this seed organism is internally compatible as one coherent sectioned design.

=== GENOME SECTION SCHEMA ===
The schema below is authoritative for the structure and meaning of CORE_GENES.
{genome_schema}

=== CANDIDATE GENETIC CODE ===
{candidate_genetic_code}

=== CANDIDATE CHANGE_DESCRIPTION ===
{candidate_change_description}

=== TASK ===
Decide whether the candidate is internally coherent as one organism.
The candidate must explicitly remain a unit-square packing design for exactly 26 total circles.
Focus not only on contradiction-free structure, but on whether the sections define a plausible score-bearing mechanism worth implementing.
Reject organisms whose section set is coherent but too over-specified or too loosely coupled to admit a compact faithful implementation.
Reject organisms whose geometric generator is an arithmetic puzzle, such as inconsistent row counts, reflection multiplicities, or role counts that do not clearly yield the count claimed by the candidate.
Reject candidates whose geometry and radius sections only describe a generic safe one-radius baseline instead of a specific score-bearing packing family.
Focus on section completeness, cross-section consistency, support for the claimed hypothesis, and whether any code-like detail remains local rather than becoming a full implementation.
Check the candidate claim-by-claim: every mechanism named in `CHANGE_DESCRIPTION` must be present in `CORE_GENES`, and every generator/count/spacing assumption in `INIT_GEOMETRY` must agree with `RADIUS_POLICY`, `PARAMETERS`, and `OPTIONAL_CODE_SKETCH`.
