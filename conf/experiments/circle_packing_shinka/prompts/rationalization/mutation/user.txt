Produce the design rationale for the next mutation child of this circle-packing organism.

=== PARENT FITNESS SIGNAL ===
{parent_fitness_signal}

=== PARENT GENETIC CODE ===
{parent_genetic_code}

=== PARENT IMPLEMENTATION (ground truth Python) ===
Below is the parent's actual `implementation.py` — the code that produced the score above. Diagnose the `WEAKNESS_HYPOTHESIS` against this code, not just the prose `## CORE_GENES` description: the description and the code can drift apart.

```python
{parent_implementation}
```

=== PARENT LINEAGE SUMMARY ===
Past idea changes in this lineage and their outcomes (higher score = better).
{parent_lineage_summary}

=== TOP-K INSPIRATION PROGRAMS (same island, sorted best first) ===
Highest-scoring organisms currently surviving on the parent's island, excluding the parent. Use them as reference programs to spot common patterns. If this block reads "(no inspirations)" the survivor pool is too small to sample yet.
{inspirations}

=== LINEAGE REGIME HINT ===
Auto-generated convergence diagnosis from the last several ancestors.
{lineage_regime_hint}

=== NOVELTY REJECTION FEEDBACK (only present on retries) ===
If a previous candidate child was rejected for lacking novelty, the critique is below. Adjust your `## CHILD_DIRECTION` to address it.
{novelty_rejection_feedback}

=== TASK ===
Use the `PARENT FITNESS SIGNAL` block above as the primary anchor: the `gap` between the parent and the best ancestor tells you whether this lineage is improving (gap near 0 — propose a forward step) or regressing (positive gap — something the best ancestor had was lost; diagnose what). Diagnose what is currently NOT being solved well by the parent. Propose a direction for the next child that (a) materially changes at least one of the four axes (packing family, radius regime, symmetry family, repair family) and (b) plausibly increases the sum of radii.

Removing inherited machinery is a first-class move. Adding mechanisms you think of yourself is a first-class move. Coefficient retuning inside the existing regime is NOT a meaningful leap and should be avoided.

Fill out the six sections in the system-prompt order. Do not emit `## CORE_GENES` — that is the next call's job.
