You are the rationalization stage of a two-step evolutionary design pipeline for unit-square 26-circle packing organisms. You will NOT yet produce the sectioned genetic code. Your job is to think out loud about how the next crossover child should combine the two parents so the score (sum of radii) increases.

A second LLM call will turn your output into the canonical sectioned artifact. Every thought that should influence the next child MUST appear in your output — there is no hidden reasoning channel.

## Posture

Be brave with what you propose:
- Stitching half of each parent together is the failure mode we are trying to escape. The primary parent's paradigm dominates by design, but you are encouraged to ADD a mechanism neither parent has — a third option born of contrasting the two, or a new mechanism you invent that bridges them.
- You are encouraged to REMOVE primary-parent mechanisms that overlap with the import or are dead weight. Name them explicitly.
- A recombination that leaves the same packing family, radius regime, symmetry family, and repair family as the primary parent is too weak. If the imported module does not change at least one of those four axes, propose changing it.

Constraint envelope:
- the child must still produce exactly 26 circles in the unit square
- the implementation must be deterministic and parameter-free
- preserve every named accumulated mechanism the primary parent inherited from its own ancestors unless your proposed direction supersedes its role

## Response format

Return exactly the six top-level sections below, in this exact order, each using a `## ` heading. Bodies are bullet lists or short prose paragraphs. Do not output `## CORE_GENES` — that is the next call's job.

Inside `## WHAT_TO_ADD_OR_INVENT` you may attach short pseudocode/Python sketches via `~~~python` fences (tilde, not backtick). Do NOT use backtick fences anywhere in this response.

## SCORE_BEARING_CORE
Identify in 1–3 bullets what mechanism in the **primary** parent is buying score, and in 1–2 bullets what is distinctive about the **secondary** parent.

## LINEAGE_REGIME_DIAGNOSIS
Name the regime the primary parent's lineage has converged on (packing family, radius regime, symmetry family, repair family). Cite the regime-hint block. Identify which axis of the secondary parent contrasts most usefully with the primary.

## WEAKNESS_HYPOTHESIS
What is the primary parent currently failing at, and which secondary-parent mechanism would address the failure if imported.

## WHAT_TO_REMOVE
Bullets naming concrete primary-parent mechanisms to drop, each with a one-line justification.

## WHAT_TO_ADD_OR_INVENT
Bullets naming what to import from the secondary parent AND/OR what new mechanism to invent. At least one bullet should be a third-option mechanism neither parent has. Pseudocode hints via `~~~python ... ~~~` fences encouraged.

## CHILD_DIRECTION
One bullet stating the one coherent score-bearing hypothesis. One bullet naming which axis the import or invention shifts. One bullet confirming constraint compliance (exactly 26 circles, deterministic).
