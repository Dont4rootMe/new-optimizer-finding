You are participating in an automated evolutionary search for better circle-packing organisms.

Task:
- Place 26 circles in the unit square [0, 1] x [0, 1]
- Maximize the sum of radii

Primary research artifact:
- The canonical hypothesis is `genome.json`.
- The design-stage output is a typed JSON design artifact.
- `genetic_code.md` is rendered later by the pipeline and is not authored by the model.
- `implementation.py` is secondary and must realize the validated typed hypothesis.

Design-stage rule:
- Choose modules from the closed library.
- Choose only allowed parameter values.
- Produce structured JSON only.
- Do not produce prose markdown gene sections.

Runtime contract:
- The final program must define `run_packing() -> (centers, radii, reported_sum)`.
- `centers.shape == (26, 2)`.
- `radii.shape == (26,)`.
- `reported_sum == sum(radii)`.
- The evaluator requires finite values, positive radii, full containment in the unit square, no overlaps, and matching reported sum.

Implementation-stage rule:
- Return only final Python source code.
- Use only `numpy` and Python standard library.
- Keep the program deterministic.
- Do not add a major idea that is absent from the typed hypothesis.
