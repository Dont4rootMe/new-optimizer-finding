# INIT_GEOMETRY
This section defines how the organism proposes the initial circle-center arrangement before any later adjustment or repair. Put genes here that describe the geometric construction of the starting layout, such as global layout family, symmetry assumptions, circle role partitioning, boundary-first versus interior-first allocation, defect placement, or other center-generation principles that materially influence attainable radii. Content in this section should stay focused on how centers are initially proposed, not on how radii are grown later and not on how violations are repaired later. This section may need to be coordinated with `RADIUS_POLICY` and `REPAIR_POLICY` when one hypothesis couples starting geometry, feasible slack, and repair behavior. Plain language, mathematical descriptions, and pseudocode-like procedural descriptions are allowed here. Prefer low-parameter geometric structure over richly over-specified role taxonomies unless the extra role structure is truly necessary for score. Full Python code is not allowed here.

# RADIUS_POLICY
This section defines how the organism assigns initial radii or role-dependent radii to circles, given the intended geometric arrangement. Put genes here that describe role-dependent radii, initial feasible slack choices, non-uniform radii when geometrically justified, and how radius assignment depends on spacing, local free space, or structural role so that later expansion has real support. This section is about radius-definition ideas, not about the later iterative growth process and not about conflict repair. It is allowed to carry a real geometric operating regime rather than bland prose such as "uniform" or "non-uniform" without further substance. Plain language, mathematical descriptions, and structured numeric reasoning are allowed here. Radius policy should usually be expressed as one compact role-dependent rule set, not as many loosely coupled exceptions. Full Python code is not allowed here.

# EXPANSION_POLICY
This section defines how the organism tries to increase radii after initialization. Put genes here that describe the real score-driving enlargement logic: where and when growth is attempted, whether growth is global, selective, or role-dependent, how growth pressure is adapted after conflicts, what should happen after a successful repair, and whether repaired regions can regrow. This section is specifically about growth dynamics after initialization. It should not redefine the initial layout and should not contain the main conflict-resolution logic. This section should not collapse into a generic slogan like "grow radii gradually"; it should describe the actual regime that could move score. Plain language, mathematical descriptions, pseudocode-like descriptions, and numeric schedules are allowed here. Prefer one clear growth regime over multiple nested growth sub-mechanisms unless the interaction is essential. Full Python code is not allowed here.

# CONFLICT_MODEL
This section defines what the organism treats as a violation, constraint breach, or optimization-relevant conflict during packing. Put genes here that describe how overlaps are identified, how boundary violations are interpreted, which conflicts matter most, whether the tracked structure is pairwise, boundary, graph-based, or hybrid, how severity is ranked, what quantities measure severity, and what summary statistics or ordering rules are maintained during the algorithm. This section is about defining conflicts, not about how they are fixed. It should define the quantities used by repair and control, not merely say that conflicts are overlaps. Plain language, mathematical descriptions, and pseudocode-like descriptions are allowed here. Conflict severity definitions must be mathematically well-posed and aligned with repair, not just verbally plausible. Full Python code is not allowed here.

# REPAIR_POLICY
This section defines how the organism corrects overlaps, boundary violations, or other conflicts once they have been detected. Put genes here that describe repair operators such as displacement, local rearrangement, repulsion, worst-conflict-first correction, sequential repair, propagated repair, conflict-priority repair, role-aware repair, or other deterministic feasibility-restoration logic that tries to preserve score. This section should preserve radii whenever possible; radius shrink is allowed only when necessary and should not be the default global cleanup mechanism. Global terminal shrink-to-feasibility is normally not a repair policy and should not be used unless the organism's actual hypothesis is built around shrink-based correction. This section may contain local procedural detail and may be more operational than the earlier sections, but it must still describe a repair mechanism rather than a full end-to-end implementation. Plain language, mathematical descriptions, pseudocode-like descriptions, numeric choices, and if truly local to repair only, short local code-like sketches are allowed here. Do not place a full algorithm implementation in this section.

# CONTROL_POLICY
This section defines the outer control logic of the organism: how phases are ordered, when growth and repair alternate, when conflicts are re-ranked, when the algorithm switches modes, when it stops, and what counts as no more profitable feasible updates. Put genes here that describe stage sequencing, acceptance rules, convergence checks, iteration budgets, escalation from coarse to fine repair, or similar global control decisions. This section is about algorithm-level control flow, not local geometry construction and not low-level repair mechanics. It is not the place for a mandatory terminal global shrink-to-feasibility doctrine unless the organism's actual hypothesis requires that. Plain language, mathematical descriptions, pseudocode-like descriptions, and numeric thresholds are allowed here. Control should remain minimal and decisive; avoid overly baroque multi-phase orchestration unless it is essential to the mechanism. Full Python code is not allowed here.

# PARAMETERS
This section stores parameter blocks, thresholds, schedules, coefficients, ratios, role-dependent constants, and other numeric or formula-level settings that the implementation is expected to realize causally. Put genes here that make the organism's quantitative choices more specific. Parameter choices may support any earlier or later mechanism; they are not decorative comments and should not read like a late appendix. This section may contain numbers, formulas, inequalities, ranges, ratios, ordering constraints, or named parameter bundles in plain text. Parameter blocks should stay compact and grouped; do not explode the organism into many loosely connected constants. This section should not introduce a brand-new algorithmic idea that properly belongs in another section. Full Python code is not allowed here.

# OPTIONAL_CODE_SKETCH
This section is optional in spirit but must always exist in the schema. It is reserved for local executable or near-executable sketches that help preserve heritable procedural detail for one module only. Content here may include pseudocode, local Python-like fragments, or patch-style code sketches, but only when they are narrowly scoped to a single module or mechanism already defined by the other sections. This section is for local inherited procedural detail, not for a generic emergency feasibility reset, not for a hidden global algorithm override, and not for a universal terminal safety clamp. This section must never contain a full `implementation.py`, must never redefine the whole organism end to end, and must never replace the higher-level genetic descriptions in the earlier sections. Prefer `- None.` unless a genuinely local executable sketch is necessary to preserve heritable procedural detail. Use this section only for localized inheritable procedural detail.
