# INIT_GEOMETRY
This section defines how the organism proposes the initial circle-center arrangement before any later adjustment or repair. Put genes here that describe the geometric construction of the starting layout, such as lattice structure, symmetry assumptions, boundary-first versus interior-first placement, defect placement, or other center-generation principles. Content in this section should stay focused on how centers are initially proposed, not on how radii are grown later and not on how violations are repaired later. Plain language, mathematical descriptions, and pseudocode-like procedural descriptions are allowed here. Full Python code is not allowed here.

# RADIUS_POLICY
This section defines how the organism assigns initial radii or role-dependent radii to circles, given the intended geometric arrangement. Put genes here that describe whether radii are uniform or non-uniform, whether different geometric roles receive different radii, and how radius assignment depends on spacing, local free space, or structural role. This section is about radius-definition ideas, not about the later iterative growth process and not about conflict repair. Plain language, mathematical descriptions, and structured numeric reasoning are allowed here. Full Python code is not allowed here.

# EXPANSION_POLICY
This section defines how the organism tries to increase radii after initialization. Put genes here that describe additive or multiplicative growth logic, density-aware expansion, staged expansion, selective expansion, or other rules that attempt to improve packing score by enlarging circles over time. This section is specifically about growth dynamics after initialization. It should not redefine the initial layout and should not contain the main conflict-resolution logic. Plain language, mathematical descriptions, pseudocode-like descriptions, and numeric schedules are allowed here. Full Python code is not allowed here.

# CONFLICT_MODEL
This section defines what the organism treats as a violation, constraint breach, or optimization-relevant conflict during packing. Put genes here that describe how overlaps are identified, how boundary violations are interpreted, what quantities measure severity, and what structures or summaries of conflicts are tracked during the algorithm. This section is about defining conflicts, not about how they are fixed. Plain language, mathematical descriptions, and pseudocode-like descriptions are allowed here. Full Python code is not allowed here.

# REPAIR_POLICY
This section defines how the organism corrects overlaps, boundary violations, or other conflicts once they have been detected. Put genes here that describe repair operators such as shifting, shrinking, repulsion, worst-conflict-first correction, sequential repair, propagated repair, or other deterministic feasibility-restoration logic. This section may contain local procedural detail and may be more operational than the earlier sections, but it must still describe a repair mechanism rather than a full end-to-end implementation. Plain language, mathematical descriptions, pseudocode-like descriptions, numeric choices, and if truly local to repair only, short local code-like sketches are allowed here. Do not place a full algorithm implementation in this section.

# CONTROL_POLICY
This section defines the outer control logic of the organism: how phases are ordered, when the algorithm switches modes, when it stops, and what progress or convergence signals it uses. Put genes here that describe stage sequencing, acceptance rules, convergence checks, iteration budgets, escalation from coarse to fine repair, or similar global control decisions. This section is about algorithm-level control flow, not local geometry construction and not low-level repair mechanics. Plain language, mathematical descriptions, pseudocode-like descriptions, and numeric thresholds are allowed here. Full Python code is not allowed here.

# PARAMETERS
This section stores explicit parameter choices, schedules, thresholds, relative coefficients, role-dependent constants, and other numeric or formula-level settings used by the other sections. Put genes here that make the organism's quantitative choices more specific. This section may contain numbers, formulas, inequalities, ranges, ratios, ordering constraints, or named parameter bundles in plain text. This section should not introduce a brand-new algorithmic idea that properly belongs in another section. Full Python code is not allowed here.

# OPTIONAL_CODE_SKETCH
This section is optional in spirit but must always exist in the schema. It is reserved for local executable or near-executable sketches that help preserve heritable procedural detail for one module only. Content here may include pseudocode, local Python-like fragments, or patch-style code sketches, but only when they are narrowly scoped to a single module or mechanism already defined by the other sections. This section must never contain a full `implementation.py`, must never redefine the whole organism end to end, and must never replace the higher-level genetic descriptions in the earlier sections. Use this section only for localized inheritable procedural detail.
