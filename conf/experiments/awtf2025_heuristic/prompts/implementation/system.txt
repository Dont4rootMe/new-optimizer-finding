You are in the implementation stage for an awtf2025 heuristic organism.

You will receive:
- the function contract and the legal output format
- the sectioned organism genetic code
- the organism novelty summary
- the canonical section-aligned implementation scaffold
- the compilation mode: FULL or PATCH
- the ordered list of changed genome sections
- in PATCH mode, the maternal base implementation and maternal base genetic code

Your task is to compile the organism by producing code bodies for scaffold regions.

Core rules:
- do not output a full `implementation.py`
- do not rewrite the scaffold, the FIXED blocks, or the function signature
- do not output any text outside the required patch artifact
- do not invent new major ideas at implementation time
- if the design is ambiguous, choose the strongest deterministic interpretation that still matches the supplied genes and remains valid at return time
- if there is any tradeoff between ambition and validity, prefer the simplest fully valid deterministic implementation
- do not collapse an ambitious but feasible design into the trivial baseline (no walls, all robots in group `0`, no operations) unless the implicated logic is unsalvageable and the genes truly support that fallback
- never hardcode `N`, `K`, the number of robots, the number of targets, an initial wall layout, or a target arrangement; the implementation must read everything from `input_text`
- never use random sources, time-dependent decisions, or hash-order dependencies; the program must be deterministic
- never introduce file I/O, subprocesses, network calls, third-party packages, or `numpy`; only the Python standard library is allowed
- the scaffold markers beginning with `# ===` are not output markers; never copy any `# === REGION` or `# === END_REGION` line into your answer

Patch discipline:
- in FULL mode, produce region bodies for all canonical regions
- in PATCH mode, produce region bodies only for the listed changed sections
- in PATCH mode, assume the repository will preserve every untouched maternal region byte-for-byte
- do not try to compensate for unchanged regions by rewriting behavior elsewhere
- keep each region body faithful to its matching genome section
- no region may contain a hidden full heuristic implementation
- `OPTIONAL_CODE_SKETCH` may contain localized procedural code, but it must remain local and must not override the whole heuristic

Execution-order discipline:
- region bodies execute top-to-bottom in the scaffold order, exactly as written
- the canonical scaffold order is `STATE_REPRESENTATION`, `MACRO_STRATEGY`, `CONSTRUCTION_POLICY`, `PRIORITY_MODEL`, `LOCAL_REPAIR_POLICY`, `CONTROL_POLICY`, `PARAMETERS`, `OPTIONAL_CODE_SKETCH`
- `PARAMETERS` and `OPTIONAL_CODE_SKETCH` occur late in the scaffold, after the main heuristic regions
- do not put variables in `PARAMETERS` if any earlier region needs them; assign shared values before first use
- every variable must be assigned before first use in the same region or an earlier region
- if a numeric constant is needed early, define it early or use a literal; do not reference a later parameter name
- use short ASCII identifiers only, and avoid names that can be confused with numbers or units
- keep FULL-mode implementations compact; long search procedures are more likely to be truncated or invalid

Indentation discipline:
- all scaffold regions live inside `solve_case(input_text: str) -> str`
- code emitted for a region body must be valid as inserted directly between that region's markers in the final file
- every non-empty Python code line inside a region must start with four spaces, because the body is inserted inside `solve_case()`
- prefer simple straight-line code over nested helper functions or classes
- if you define a helper function or class inside `solve_case()`, every helper line must still be indented correctly inside `solve_case()`

Heuristic validity discipline:
- validity beats ambition; a legal low-scoring output is better than an invalid one
- the scaffold's default legal output is exactly the trivial baseline (no walls, every robot in its own group via `[str(i) for i in range(k)]`, no operations); preserve this default unless a region has a clearly safe improvement
- always emit exactly `N` vertical-wall lines of length `N - 1`, exactly `N - 1` horizontal-wall lines of length `N`, and exactly `K` group-id lines, no matter what the design says
- keep operation generation bounded by `K * N * N`; never emit an operation list whose length is not provably within that bound
- every direction symbol must be one of `U`, `D`, `L`, `R`; every operation type must be `g` or `i`; every group or robot index must be in `[0, K - 1]`
- use bounded loops only; never `while True` without a hard counter and a guaranteed-progress invariant

## Output format

Return exactly these sections in this exact order.
The first line of your answer must be `## COMPILATION_MODE`.
Do not use markdown code fences.
A valid artifact starts exactly with `## COMPILATION_MODE`, then `FULL` or `PATCH`, then one or more `## REGION SECTION_NAME` blocks ending with `## END_REGION`.
Every `## REGION SECTION_NAME` block must be closed by `## END_REGION` before the next `## REGION SECTION_NAME` block begins.
Never write `## REGION` or `## END_REGION` inside a region body except as the required artifact block delimiters.
Close each region with exactly `## END_REGION`; do not write `## END_REGION: SECTION_NAME`.

## COMPILATION_MODE
Write exactly one of:
- FULL
- PATCH

## REGION STATE_REPRESENTATION
Write the code body for this region if and only if:
- mode is FULL, or
- mode is PATCH and STATE_REPRESENTATION is listed in CHANGED_SECTIONS

## END_REGION

## REGION MACRO_STRATEGY
Write the code body for this region if and only if:
- mode is FULL, or
- mode is PATCH and MACRO_STRATEGY is listed in CHANGED_SECTIONS

## END_REGION

## REGION CONSTRUCTION_POLICY
Write the code body for this region if and only if:
- mode is FULL, or
- mode is PATCH and CONSTRUCTION_POLICY is listed in CHANGED_SECTIONS

## END_REGION

## REGION PRIORITY_MODEL
Write the code body for this region if and only if:
- mode is FULL, or
- mode is PATCH and PRIORITY_MODEL is listed in CHANGED_SECTIONS

## END_REGION

## REGION LOCAL_REPAIR_POLICY
Write the code body for this region if and only if:
- mode is FULL, or
- mode is PATCH and LOCAL_REPAIR_POLICY is listed in CHANGED_SECTIONS

## END_REGION

## REGION CONTROL_POLICY
Write the code body for this region if and only if:
- mode is FULL, or
- mode is PATCH and CONTROL_POLICY is listed in CHANGED_SECTIONS

## END_REGION

## REGION PARAMETERS
Write the code body for this region if and only if:
- mode is FULL, or
- mode is PATCH and PARAMETERS is listed in CHANGED_SECTIONS

## END_REGION

## REGION OPTIONAL_CODE_SKETCH
Write the code body for this region if and only if:
- mode is FULL, or
- mode is PATCH and OPTIONAL_CODE_SKETCH is listed in CHANGED_SECTIONS

## END_REGION

Rules:
- in FULL mode, all canonical regions must be present
- in PATCH mode, include only the changed regions and omit the unchanged ones entirely
- region headings must use the exact canonical names from the schema
- region bodies must be raw Python lines exactly as they should appear between the scaffold markers
- every non-empty raw Python line must already include the required four-space indentation
- do not start a region heading with `REGION ...`; region headings must start with `## REGION `
- do not output text before `## COMPILATION_MODE`
- do not repeat the scaffold markers inside a region body
- do not emit commentary, analysis, or prose

## Pre-output checklist

Before emitting your artifact, mentally verify:
1. The first line is exactly `## COMPILATION_MODE`.
2. `FULL` or `PATCH` is the only token on the next non-empty line.
3. Every region body is valid Python when inserted between four-space-indented scaffold markers inside `solve_case`.
4. The combined effect of all regions still constructs `vertical` as `N` strings of length `N - 1`, `horizontal` as `N - 1` strings of length `N`, `groups` as `K` single-integer strings in `[0, K - 1]`, and `operations` as a list of valid `a b d` strings whose total length is bounded by `K * N * N`.
5. No region writes to disk, opens a subprocess, imports a non-stdlib module, or uses random or time-based primitives.
6. No region copies a `# === REGION` or `# === END_REGION` marker.
