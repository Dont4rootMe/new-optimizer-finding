Compile this awtf2025 heuristic organism into scaffold region bodies.

=== FUNCTION CONTRACT ===
You must implement exactly this function:

    def solve_case(input_text: str) -> str:

Argument:
  - `input_text`: the full contest input for exactly one case

Return value:
  - one string containing the full contest output for that case

Required output structure:
  1. `N` lines of vertical added-wall bits, each length `N - 1`
  2. `N - 1` lines of horizontal added-wall bits, each length `N`
  3. `K` integers assigning every robot to a group in `[0, K - 1]`
  4. zero or more operations, each line formatted as `a b d`

Evaluator rules:
  - added walls are merged with the original walls
  - operation type `a` must be `g` or `i`
  - direction `d` must be `U`, `D`, `L`, or `R`
  - the move count must be at most `K * N * N`
  - lower absolute score is better: `T + 100 * sum_k Manhattan(final_position_k, target_k)`

=== COMPILATION MODE ===
{compilation_mode}

=== CHANGED_SECTIONS ===
{changed_sections}

=== ORGANISM GENETIC CODE ===
{organism_genetic_code}

=== ORGANISM CHANGE_DESCRIPTION ===
{change_description}

=== MATERNAL BASE GENETIC CODE ===
{base_parent_genetic_code}

=== MATERNAL BASE IMPLEMENTATION ===
{base_parent_implementation}

=== CANONICAL IMPLEMENTATION SCAFFOLD ===
{implementation_template}

=== TASK ===
Produce scaffold region bodies for this organism.

If COMPILATION_MODE is FULL:
- compile all canonical regions from the scaffold

If COMPILATION_MODE is PATCH:
- compile only the listed CHANGED_SECTIONS
- do not compile any unchanged section
- the repository will preserve unchanged maternal regions byte-for-byte

Be faithful to the supplied sectioned genes and the change description.
Do not output a full `implementation.py`.
Return only the patch artifact in the required format.
