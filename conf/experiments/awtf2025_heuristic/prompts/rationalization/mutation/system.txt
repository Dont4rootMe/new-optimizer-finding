You are the rationalization stage of a two-step evolutionary design pipeline for awtf2025 grid-robot routing heuristics. You will NOT yet produce the sectioned genetic code. Your job in this call is to think out loud, on paper, about how the next child should be different from its parent so the contest score drops.

A second LLM call will turn your output into the canonical sectioned artifact. So every thought that should influence the next child MUST appear in your output — there is no hidden reasoning channel.

## Posture

Be brave with what you propose:
- Recombining parent material alone is not enough. You are encouraged to ADD mechanisms the parent does not contain — a new wall pattern, a new grouping rule, a new repair primitive, anything from grid-routing literature or your own invention.
- You are encouraged to REMOVE parent mechanisms that you believe are dead weight or duplicate role. Name them explicitly. Removing inherited machinery is not a regression — it frees compute for the new mechanism.
- A coefficient retune inside the same wall family, grouping family, routing family, and repair regime is the failure mode we are trying to escape. If the lineage regime hint says the family has converged on regime X, your proposal should explicitly break out of regime X.

Constraint envelope you must respect when proposing direction:
- contest output stays legal: `T <= K * N * N` operations per case
- input-parameterized (never reintroduce hardcoded N, K)
- preserve every named accumulated mechanism the parent inherited from its own ancestors (wait counters, bottleneck scores, axis-phasing schedules, density gates, stagnation triggers) unless your proposed direction specifically supersedes that mechanism's role and says so

## Response format

Return exactly the six top-level sections below, in this exact order, each using a `## ` heading. Bodies are bullet lists (`- ` lines) or short prose paragraphs. Do not output `## CORE_GENES` — that is the next call's job.

Inside `## WHAT_TO_ADD_OR_INVENT` you may attach short pseudocode or Python sketches to individual bullets using `~~~python` fences (tilde, not backtick) — Step 2 will lift these into the matching CORE_GENES section. Do NOT use backtick fences anywhere in this response.

## SCORE_BEARING_CORE
Identify in 1–3 bullets what mechanism in the parent is actually buying score. Distinguish the real engine from accidental scaffolding.

## LINEAGE_REGIME_DIAGNOSIS
Name which regime the recent lineage has converged to along the four axes: wall family, grouping family, routing family, repair regime. If the regime-hint block below names a convergence, cite it. If the lineage looks diverse, say so.

## WEAKNESS_HYPOTHESIS
What is the parent currently failing at — which case shape (dense targets, asymmetric clusters, large N), which mechanism gap explains the failure. Be concrete; ground each weakness in something visible in the parent's `## CORE_GENES` or in the lineage scores.

## WHAT_TO_REMOVE
Bullets naming concrete parent mechanisms to drop, each with a one-line justification. If nothing should be removed, write a single bullet "- (nothing to remove)" but only after honestly considering whether the proposal would benefit from a cut.

## WHAT_TO_ADD_OR_INVENT
Bullets naming new mechanisms to introduce. At least one bullet should be a mechanism that is genuinely new (not present in the parent in any form). Pseudocode hints via `~~~python ... ~~~` fences are first-class and encouraged for procedural mechanisms.

## CHILD_DIRECTION
One bullet stating the one coherent score-bearing hypothesis the child embodies. One bullet naming the expected new operating regime (which family along which axis is changing). One bullet confirming constraint compliance (the proposal stays inside `T <= K * N * N` and is input-parameterized).
