Create one crossover child from these two awtf2025 heuristic parents.

=== GENOME SECTION SCHEMA ===
The schema below is authoritative for the structure and meaning of CORE_GENES.
{genome_schema}

=== STEP 1 DESIGN RATIONALE ===
A prior rationalization call produced the design plan below. Materialize WHAT_TO_REMOVE, WHAT_TO_ADD_OR_INVENT and CHILD_DIRECTION in the structured artifact. If the block reads "(rationalization disabled — single-call mode)", proceed without it.
{rationalization}

=== PRIMARY PARENT GENETIC CODE ===
Use this as the structural base for the child. Its core paradigm should dominate the recombination.
{mother_genetic_code}

=== PRIMARY PARENT IMPLEMENTATION (ground truth Python) ===
The actual `implementation.py` the primary parent runs — the code the host evaluator scored. This is what the next stage will use as the baseline for compiling the child's Python.

```python
{mother_implementation}
```

=== PRIMARY PARENT LINEAGE SUMMARY ===
Scores in lineage: higher is better. Use score trends to identify which mechanisms in the primary parent's ancestry actually helped.
{mother_lineage_summary}

=== SECONDARY PARENT GENETIC CODE ===
Use this as the donor of one coherent imported module that complements the primary parent's structure.
{father_genetic_code}

=== SECONDARY PARENT IMPLEMENTATION (ground truth Python) ===
The actual `implementation.py` the secondary parent runs. When picking the imported module, confirm against this code that the mechanism really exists in the donor.

```python
{father_implementation}
```

=== SECONDARY PARENT LINEAGE SUMMARY ===
Scores in lineage: higher is better. Use score trends to identify which mechanisms in the secondary parent's ancestry actually helped.
{father_lineage_summary}

=== NOVELTY REJECTION FEEDBACK ===
If a previous candidate child was rejected for lacking novelty, address those issues explicitly.
If there was no prior rejection, this section will say so.
{novelty_rejection_feedback}

=== TASK ===
Design one crossover child from these two parents.
The result must be a full sectioned genetic code in the canonical format, internally consistent, and summarized by one unified novelty statement for lineage.
The child remains an awtf2025 heuristic organism and must still produce a contest-legal output that respects the explicit output shape and the operation budget `T <= K * N * N`.
Build a primary-parent-dominant organism with one coherent score-bearing imported module from the secondary parent.
Pick exactly one module from the secondary parent worth importing. Do not synthesize many partial ideas across both parents.
The imported module must change the expected wall, grouping, routing, or repair regime, not only the provenance story.
If both parents still imply the same effective regime, the crossover should escape that convergence by importing or designing one component that introduces a different family rather than tuning inside the shared regime.
Verbatim cloning of the primary parent is invalid — at least one CORE_GENES section must contain a recombination that neither parent presents on its own.
If novelty rejection feedback is present, resolve it explicitly instead of rephrasing one parent.
Return the structured design directly. Do not add preface text before `## CORE_GENES`.
