You are creating one crossover child from two parent organisms.

## Crossover philosophy

Crossover takes the higher-scoring parent's core structure and enriches it with one or two complementary ideas from the secondary parent.
It is NOT a 50/50 merge. The primary parent's algorithmic paradigm should dominate.
The secondary parent contributes a specific refinement, not a competing paradigm.

Priority order:
1. analyze the child genetic code draft already selected by evolution
2. use parent genetic codes and lineage to understand idea origin
3. use parent implementations only as reference material

The first parent is primary. The second parent is secondary.

Do:
- treat the child draft as the main object
- if the selected child draft already forms a coherent recombination, you may keep it largely intact and only clarify or lightly refine it
- keep the primary parent's core algorithmic structure intact
- incorporate at most one or two specific ideas from the secondary parent that complement the primary structure
- compare parent scores in lineage to judge which parent's ideas contributed more value
- produce one unified novelty summary for the child

Do not:
- replace the primary parent's core paradigm with the secondary parent's
- try to merge two incompatible algorithmic approaches into one organism
- concatenate parent code
- collapse the child back into one parent because that parent implementation looks more complete
- let either parent implementation override the selected child draft
- turn the response into a direct solved output instead of a gene-level design
- bundle multiple independent ideas into one gene — keep genes atomic
- include Python code, variable names, or hardcoded constants in genes or summaries

## Response format

Return the following sections, each starting with `## SECTION_NAME`.
Do NOT use markdown code fences.

## CORE_GENES
At least 3 bullets, at most 7.
Each bullet is one atomic idea (one independent concept per bullet).
Write ideas in plain language or math. No Python code, no variable names, no hardcoded constants.

## INTERACTION_NOTES
State what assumptions the combined child makes and where it is likely to help or fail.

## COMPUTE_NOTES
State the computational style implied by the combined child design.

## CHANGE_DESCRIPTION
Give one unified novelty summary describing what was inherited, synthesized, or redirected.
Include: (1) which parent's ideas dominate and why, (2) what the combined hypothesis is, (3) what score improvement is expected and why.
This must be a plain-language statement of the idea. No code, no implementation details.
