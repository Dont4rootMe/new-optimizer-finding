Evaluate whether this seed awtf2025 heuristic is internally compatible as one coherent sectioned design.

=== GENOME SECTION SCHEMA ===
The schema below is authoritative for the structure and meaning of CORE_GENES.
{genome_schema}

=== CANDIDATE GENETIC CODE ===
{candidate_genetic_code}

=== CANDIDATE CHANGE_DESCRIPTION ===
{candidate_change_description}

=== TASK ===
Decide whether the candidate is internally coherent as one heuristic.
The candidate must explicitly remain an awtf2025 heuristic organism whose implementation will produce a contest-legal output for any input case, parameterized only by the input.
Focus not only on contradiction-free structure, but on whether the sections define a plausible score-bearing mechanism worth implementing.
Reject organisms whose section set is coherent but too over-specified or too loosely coupled to admit a compact faithful implementation in pure standard-library Python within the operation budget `T <= K * N * N`.
Reject organisms whose construction or routing depends on hardcoded `N`, `K`, initial wall layouts, or target arrangements rather than reading them from the input.
Reject candidates whose state representation and macro strategy only describe the trivial baseline (no walls, all robots in group `0`, no operations) instead of a specific score-bearing wall, grouping, or routing family.
Focus on section completeness, cross-section consistency, support for the claimed hypothesis, and whether any code-like detail remains local rather than becoming a full implementation.
Check the candidate claim-by-claim: every mechanism named in `CHANGE_DESCRIPTION` must be present in `CORE_GENES`, and every assumption in `STATE_REPRESENTATION` must agree with `MACRO_STRATEGY`, `CONSTRUCTION_POLICY`, `PRIORITY_MODEL`, `LOCAL_REPAIR_POLICY`, `CONTROL_POLICY`, `PARAMETERS`, and `OPTIONAL_CODE_SKETCH`.
