Evaluate whether this mutation child is internally compatible as one coherent sectioned design.

=== GENOME SECTION SCHEMA ===
The schema below is authoritative for the structure and meaning of CORE_GENES.
{genome_schema}

=== CHILD GENETIC CODE DRAFT SELECTED BY EVOLUTION ===
{inherited_gene_pool}

=== EXCLUDED IDEAS ===
These ideas were explicitly removed during mutation sampling.
{removed_gene_pool}

=== PARENT GENETIC CODE ===
{parent_genetic_code}

=== CANDIDATE CHILD GENETIC CODE ===
{candidate_genetic_code}

=== CANDIDATE CHILD CHANGE_DESCRIPTION ===
{candidate_change_description}

=== TASK ===
Decide whether the candidate is internally coherent as one heuristic and remains a plausible targeted inherited modification of the selected child draft rather than a hidden restart.
The child must explicitly remain an awtf2025 heuristic organism whose implementation will produce a contest-legal output for any input case, parameterized only by the input.
Focus not only on contradiction-free structure, but on whether the sections define a plausible score-bearing mechanism worth implementing.
Reject children whose changed sections are coherent on paper but too underconstrained or too over-elaborated to implement faithfully within the operation budget `T <= K * N * N`.
Reject a candidate whose claimed improvement depends on a new wall, grouping, routing, or repair regime but whose supporting sections still preserve the old phenotype family.
Reject a mutation that leaves the same wall family, the same grouping family, the same routing family, and the same repair regime in place while only changing story or support text.
Focus on section completeness, cross-section consistency, support for the claimed hypothesis, and whether any code-like detail remains local rather than becoming a full implementation.
Check `CHANGE_DESCRIPTION` against the parent, excluded ideas, and child sections: every claimed mutation must be visible in `CORE_GENES`, and no claim may describe retained or excluded material as a new mutation.
Do not decide novelty here.
