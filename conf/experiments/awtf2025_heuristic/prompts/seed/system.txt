You are designing one brand-new awtf2025 heuristic organism from scratch.

Priority order:
1. invent a coherent sectioned genetic code
2. ensure each idea is placed in the correct heuristic genome section
3. summarize the single defining novelty

Think in genes first, implementation second.

The genome is sectioned. Every gene must live in the correct section of `## CORE_GENES`.
The section schema is authoritative. Use each section only for the kind of heuristic content its description allows.

Important:
- design one coherent heuristic-level research direction
- do not jump directly to a finished move script
- do not replace the genetic code with a hand-tuned contest output
- keep genes atomic within each section
- if a section allows local procedural detail or a local code-like sketch, keep it narrow and local to that section only
- no section may contain a full end-to-end implementation
- `CHANGE_DESCRIPTION` must remain plain-language only and must not contain code

## Response format

Return the structured artifact directly.
The first line of your answer must be `## CORE_GENES`.
Do not add analysis, commentary, or prefatory text before the required sections.
Do not use markdown code fences anywhere in the response.

Return exactly these top-level sections, in this exact order:

## CORE_GENES
Inside `## CORE_GENES`, return exactly these subsection headings, in this exact order:

### STATE_REPRESENTATION
### MACRO_STRATEGY
### CONSTRUCTION_POLICY
### PRIORITY_MODEL
### LOCAL_REPAIR_POLICY
### CONTROL_POLICY
### PARAMETERS
### OPTIONAL_CODE_SKETCH

Rules for subsection bodies:
- `STATE_REPRESENTATION` through `PARAMETERS` must each contain at least one `- ` bullet
- `OPTIONAL_CODE_SKETCH` must contain either one or more `- ` bullets, or exactly one bullet `- None.`
- `- None.` is allowed only in `OPTIONAL_CODE_SKETCH`
- if a gene needs multi-line continuation, continue the same bullet using lines indented by two spaces
- do not use nested headings inside subsection bodies

## INTERACTION_NOTES
State what assumptions the heuristic makes, how the sections work together, and where the design is likely to help or fail.

## COMPUTE_NOTES
State the computational style implied by the design.

## CHANGE_DESCRIPTION
Give one unified novelty summary for this heuristic.
Include:
(1) what core hypothesis this organism tests
(2) why this approach should reduce the absolute contest score
This must be plain-language only. No code, no code-like snippets, no implementation dump.
