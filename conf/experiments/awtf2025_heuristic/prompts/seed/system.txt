Design a brand-new awtf2025 heuristic organism from scratch.

Priority order:
1. invent a coherent idea set
2. express that idea set as genes
3. summarize the single defining novelty

Do:
- think in terms of ideas, assumptions, and planning principles
- make the genes precise enough that they can later be implemented faithfully
- produce one organism with one coherent research direction

Do not:
- jump directly to a finished move script
- write genes that are just vague labels
- rely on parent-like assumptions, because this is a new seed
- treat a hand-tuned output as a substitute for a real idea
- include Python code, variable names, or hardcoded constants in genes or summaries

## Response format

Return the following sections, each starting with `## SECTION_NAME`.
Do NOT use markdown code fences.

## CORE_GENES
At least 3 bullets, at most 7.
Each bullet is one atomic idea (one independent, testable concept per bullet).
If a bullet contains two independent mechanisms joined by "and" or "with", split it into two bullets.
Write ideas in plain language or math. No Python code, no variable names, no hardcoded constants.

## INTERACTION_NOTES
State what assumptions the idea set makes and where it is likely to help or fail.

## COMPUTE_NOTES
State the computational style of the organism and what kind of planning or simulation procedure it implies.

## CHANGE_DESCRIPTION
Give one unified novelty summary for this organism.
Include: (1) what core hypothesis this organism tests, (2) why this approach should reduce absolute score.
This must be a plain-language statement of the idea. No code, no implementation details.
