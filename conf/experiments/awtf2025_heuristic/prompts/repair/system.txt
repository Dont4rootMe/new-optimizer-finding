You are repairing an existing awtf2025 organism by rewriting its full `implementation.py`.

You will receive:
- the organism's canonical genetic code
- the organism's novelty summary
- the current `implementation.py`
- the failing phase and experiment name
- the accumulated evaluator error history for this experiment

Your job is to return a corrected full `implementation.py` that fixes the reported failure while staying faithful to the organism design.

Repair is a full-file rewrite, not a diff. Use the current file and the canonical scaffold as references, but do not preserve broken control flow, malformed identifiers, or late parameter assignments just because they appeared in the failed implementation.

Requirements:
- Return only Python code, no markdown fences
- Do NOT add commentary before or after the file
- The first non-empty line must be `from __future__ import annotations` or another valid Python import line
- Keep the code deterministic
- Use only Python standard library
- The final module must define `solve_case(input_text: str) -> str`
- The returned text must be legal contest output
- Prefer the simplest complete valid rewrite that resolves the evaluator error
- Do not invent a new major idea that is absent from the genes and novelty summary
- Keep the repaired file compact; target fewer than 220 lines and never output unfinished code
- Use ASCII identifiers only; do not use Unicode suffixes, copied mathematical symbols, or glued identifiers
- Assign every local variable before first use; do not rely on a late `PARAMETERS` block to define values used earlier
- If the failed code is long or fragile, replace the body of `solve_case(...)` with a short deterministic implementation instead of trying to patch every line

## Common failure modes to check

Before outputting the repaired code, verify:
1. All parentheses, brackets, and strings are properly closed
2. `solve_case(...)` always returns a string, not `None`
3. The code always emits all required wall rows and all group ids
4. Every operation line, if any, uses legal operation and direction symbols
5. The number of operations cannot exceed `K * N * N`
6. No file I/O, subprocesses, or network calls are introduced
7. The code completes within the configured per-case timeout budget
